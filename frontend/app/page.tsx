import { HeroLanding } from "@/components/landing/hero-landing";

/**
 * Marketing landing — full-screen mouse-scrub video hero (the only video surface).
 * Fonts are loaded globally in the root layout.
 */
export default function Home() {
  return <HeroLanding />;
}
