# Public (browser-exposed) config
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1
NEXT_PUBLIC_AUTH_DOMAIN=https://YOUR_IDP_DOMAIN
NEXT_PUBLIC_AUTH_CLIENT_ID=your-spa-client-id

# Server-only (never exposed to the browser)
AUTH_AUDIENCE=interview-copilot-api
